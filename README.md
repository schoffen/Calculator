# 🧮 Console Calculator in C#

A simple **C# (.NET 9)** console calculator that performs basic operations (addition, subtraction, multiplication, and division) and logs all operations to a **JSON** file for history tracking.

---

## 🚀 Features

- Available operations:  
  ➕ Addition (`a`)  
  ➖ Subtraction (`s`)  
  ✖️ Multiplication (`m`)  
  ➗ Division (`d`)

- Input validation for numeric values.  
- Exception handling with friendly error messages.  
- Automatic logging of all operations into a `calculatorLog.json` file.  
- Modular structure separating the **console interface** from the **logic library** (`CalculatorLibrary`).

This project was made following a tutorial on Microsoft Learning Documentation.
